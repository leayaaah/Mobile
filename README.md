# Mobile

Ứng dụng Expo (React Native + TypeScript) với các chức năng cơ bản để test nhanh:

- Counter: tăng, giảm, reset
- Quick Notes: thêm và xóa ghi chú

## Chạy ứng dụng

```bash
npm install
npm run start
```

Sau đó mở trên Expo Go hoặc chạy:

```bash
npm run android
npm run ios
```
